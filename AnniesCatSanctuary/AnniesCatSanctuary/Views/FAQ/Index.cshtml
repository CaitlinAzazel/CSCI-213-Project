@model IEnumerable<MyProject.Models.FAQ>
    <h2>FAQs</h2>
    <ul>
        @foreach (var faq in Model)
    {
        <li><strong>@faq.Question</strong>: @faq.Answer</li>
    }
    </ul>

    <a href="@Url.Action("Create", "FAQ")">Add New FAQ</a>
